3 Напишите программу, которая принимает на вход координаты точки (X и Y), причём X ≠ 0 и Y ≠ 0 и выдаёт номер четверти плоскости, в которой находится эта точка 
(или на какой оси она находится).

x = float(input("Введите значение x координаты: "))
y = float(input("Введите значение y координаты: "))
if x == 0 or y == 0:
    print("x или y не должны быть равны 0")
elif x > 0 and y > 0:
    print(f"Точка с координатами ({x}, {y}) находится в 1-ой четверти") 
elif x > 0 and y < 0:
    print(f"Точка с координатами ({x}, {y}) находится в 4-ой четверти")
elif x < 0 and y < 0:
    print(f"Точка с координатами ({x}, {y}) находится в 3-ей четверти")
else:
    print(f"Точка с координатами ({x}, {y}) находится во 2-ой четверти")
